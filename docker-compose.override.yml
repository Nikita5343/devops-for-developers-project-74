services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /root
    volumes:
      - ./app:/root                  # ← Volumes ТУТ!
    ports:
      - "8080:8080"
    user: "${UID:-1000}:${GID:-1000}"
    command: make dev
